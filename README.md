# Summarization API with LLM Tool Integration

A **FastAPI microservice** for generating text summarizations using **Google Gemini LLM** via the Gemini API. The service supports different **lengths**, **styles** and an optional **focus topic** to emphasize specific aspects of the text.

---

## 1. Project structure & setup instructions

**Structure of this repository**:

```bash
.
├── summarizer_backend
    ├── app
    └── ...
├── summarizer_frontend
    ├── app
    └── ...
├── requirements.txt
├── docker-compose.yml
├── README.md
└── requirements.txt
```
